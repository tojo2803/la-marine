# Copie ce fichier en .env (à la racine, à côté de docker-compose.yml) et
# remplis tes propres valeurs. Ce seul fichier configure les 3 services
# (base de données, backend, frontend).

DB_NAME=la_marine
DB_USER=la_marine_app
DB_PASSWORD=LaMarine2026
DB_PORT=5433

JWT_SECRET=35fc3048d105fa4b3715cf43bb0f32d8ccf817f2595e0bf8c1e57c751950a371
JWT_EXPIRES_IN=8h

# URL publique du frontend (sert à autoriser les requêtes CORS côté backend)
CLIENT_ORIGIN=http://localhost:8081

# URL de l'API telle qu'appelée depuis le navigateur (donc localhost, pas "backend")
VITE_API_URL=http://localhost:3000/api
